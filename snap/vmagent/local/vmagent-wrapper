!/bin/bash

test -e $SNAP_DATA/extra_flags || echo 'FLAGS=""' > $SNAP_DATA/extra_flags

# get args
. $SNAP_DATA/extra_flags
$SNAP/bin/vmagent -remoteWrite.tmpDataPath /var/lib/vmagent-remotewrite-data -promscrape.config /etc/vmagent-scrape-config.yaml $FLAGS